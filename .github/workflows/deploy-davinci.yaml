name: DaVinci CI/CD (Dev → Test → Prod)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate DaVinci Bearer Token
        id: token
        run: |
          RESPONSE=$(curl -s -X POST \
            -u "${{ secrets.TEST_DAVINCI_CLIENT_ID }}:${{ secrets.TEST_DAVINCI_CLIENT_SECRET }}" \
            -H "Content-Type: application/x-www-form-urlencoded" \
            -d "grant_type=client_credentials" \
            "https://auth.pingone.eu/${{ secrets.TEST_DAVINCI_ENVIRONMENT_ID }}/as/token")

          TOKEN=$(echo $RESPONSE | jq -r '.access_token')
          echo "token=$TOKEN" >> $GITHUB_OUTPUT

      - name: Import DaVinci Flow
        run: |
          curl -s -X POST \
            -H "Authorization: Bearer ${{ steps.token.outputs.token }}" \
            -H "Content-Type: application/json" \
            --data @flows/Change_Password.json \
            "https://auth.pingone.eu/${{ secrets.TEST_DAVINCI_ENVIRONMENT_ID }}/flows/import"

      - name: Publish DaVinci Flow
        run: |
          curl -s -X POST \
            -H "Authorization: Bearer ${{ steps.token.outputs.token }}" \
            "https://auth.pingone.eu/${{ secrets.TEST_DAVINCI_ENVIRONMENT_ID }}/flows/publish"

  ##########################
  # 1. Deploy to TEST (manual approval)
  ##########################
  deploy-test:
    name: Deploy to Test
    runs-on: ubuntu-latest
    #environment:
     # name: test
      #url: https://apps.pingone.eu/9a773ab2-5392-467e-86bc-7bb683d1ae7f/myapps/  #application portal URL
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Terraform Init
        run: terraform init
        working-directory: terraform

      - name: Terraform Plan TEST
        run: |
          terraform plan \
            -var "davinci_bearer_token=${{ steps.token.outputs.token }}" \
            -var-file="../env/test.tfvars" \
            -var "davinci_environment_id=${{ secrets.TEST_DAVINCI_ENVIRONMENT_ID }}"
        working-directory: terraform

      - name: Terraform Apply TEST
        run: |
          terraform apply -auto-approve \
            -var "davinci_bearer_token=${{ steps.token.outputs.token }}" \
            -var-file="../env/test.tfvars" \
            -var "davinci_environment_id=${{ secrets.TEST_DAVINCI_ENVIRONMENT_ID }}"
        working-directory: terraform









